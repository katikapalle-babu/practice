/*a. Write a program that takes a list of lists of integers and flattens it to a list of integers.

b. Given a list of tuples, where each tuple contains two strings, flatten the list and create a list of those strings.

c. Create a program that takes a list of lists of strings and returns a single list containing all the characters from the strings.

d. Given a list of lists of booleans, flatten the list and return a list of the unique boolean values.

*/
package com.globallogic.service
object Main extends App {
  val list = List(List(1, 2, 3, 4, 5),List(4,5,6,8),List(9,8,8))
  val listresult=list.flatten
  println(listresult)
  val tuples1=List(
    ("shobhhan","bac"),
    ("babu","k")
  )
  val list2result=tuples1.flatten
  println(list2result)

  val listOfTuples = List(
    ("hello", "world"),
    ("scala", "programming"),
    ("functional", "programming")
  )

  val flattenedStrings = listOfTuples.flatten.distinct
  println(flattenedStrings)



}




